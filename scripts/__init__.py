"""viralman scripts package — kept as a package so pip-install ships them
alongside `viralman_cli`. The .py files inside are still designed to run as
standalone executables; the `viralman` console-script dispatches to them via
subprocess (see viralman_cli/__init__.py)."""

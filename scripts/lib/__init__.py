"""Internal helpers for viralman scripts. The modules here are imported
both as top-level (via the scripts' `sys.path.insert(..., 'lib')` pattern,
e.g. `from creds import load`) and as `scripts.lib.<name>` from the
installed package surface."""
